<Window x:Name="quadro" x:Class="TNKDxf.MainWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:TNKDxf" 
      xmlns:local1="clr-namespace:TNKDxf.ViewModel"
       mc:Ignorable="d"
      Title="FLUXO DO DESENHO" Height="450" Width="1400">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DataGrid com os arquivos -->
            <DataGrid ItemsSource="{Binding Arquivos}" AutoGenerateColumns="False"
                 Grid.Column="0" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome do Arquivo" Binding="{Binding Nome}" Width="200"/>
                    <DataGridTemplateColumn Header="Ação" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Aberto, Converter={local:BoolToAbrirFecharConverter}}" 
                                   Command="{Binding DataContext.ToggleAbrirCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" 
                                   CommandParameter="{Binding}" 
                                   Margin="2" Padding="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Errado}" Value="True">
                                <Setter Property="Background" Value="LightPink"/>
                                <Setter Property="Foreground" Value="DarkRed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>

            <!-- Botão para enviar arquivos corretos -->
            <Button Content="Enviar Arquivos Corretos" 
                Command="{Binding EnviarCorretosCommand}"
                Grid.Row="1" Margin="5" Padding="10,5"
                HorizontalAlignment="Stretch"/>


        </Grid>
        <!-- TabControl para exibir os arquivos abertos -->
        <TabControl ItemsSource="{Binding Tabs}" Grid.Column="1" Margin="5">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentPresenter Content="{Binding Content}"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
