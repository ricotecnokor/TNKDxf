<Application x:Class="TNKDxf.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:TNKDxf" StartupUri="MainWindow.xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d1p1:Ignorable="d" xmlns:d1p1="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sys="clr-namespace:System;assembly=mscorlib">
  <Application.Resources>
    <ResourceDictionary>
      <!-- Color palette -->
      <Color x:Key="Color.Accent">#FF1976D2</Color>
      <Color x:Key="Color.Accent.Hover">#FF1E88E5</Color>
      <Color x:Key="Color.Accent.Pressed">#FF1565C0</Color>
      <Color x:Key="Color.Gray.Background">#FFF3F4F6</Color>
      <Color x:Key="Color.Gray.Border">#FFBDBDBD</Color>
      <Color x:Key="Color.Foreground">#DD000000</Color>
      <Color x:Key="Color.Foreground.Inverted">#FFFFFFFF</Color>
      <Color x:Key="Color.Disabled">#FF9E9E9E</Color>

      <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Accent}" />
      <SolidColorBrush x:Key="Brush.Accent.Hover" Color="{StaticResource Color.Accent.Hover}" />
      <SolidColorBrush x:Key="Brush.Accent.Pressed" Color="{StaticResource Color.Accent.Pressed}" />
      <SolidColorBrush x:Key="Brush.Gray.Background" Color="{StaticResource Color.Gray.Background}" />
      <SolidColorBrush x:Key="Brush.Gray.Border" Color="{StaticResource Color.Gray.Border}" />
      <SolidColorBrush x:Key="Brush.Foreground" Color="{StaticResource Color.Foreground}" />
      <SolidColorBrush x:Key="Brush.Foreground.Inverted" Color="{StaticResource Color.Foreground.Inverted}" />
      <SolidColorBrush x:Key="Brush.Disabled" Color="{StaticResource Color.Disabled}" />

      <!-- Tipografia: usando fonte incorporada (Montserrat). Adicione os arquivos .ttf em TNKDxf/Fonts -->
      <FontFamily x:Key="Font.Primary">/TNKDxf;component/Fonts/#Montserrat</FontFamily>
      <sys:Double x:Key="Font.Size.Small">12</sys:Double>
      <sys:Double x:Key="Font.Size.Base">13</sys:Double>
      <sys:Double x:Key="Font.Size.Large">14</sys:Double>

      <!-- System selection colors -->
      <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{StaticResource Color.Accent}" />
      <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFFFF" />

      <!-- Typography and default background -->
      <Style TargetType="{x:Type Control}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Base}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
      </Style>

      <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource Brush.Gray.Background}" />
      </Style>

      <!-- Global style for Button -->
      <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Inverted}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent.Pressed}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="MinWidth" Value="80" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Style.Triggers>
          <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background" Value="{StaticResource Brush.Accent.Hover}" />
          </Trigger>
          <Trigger Property="IsPressed" Value="True">
            <Setter Property="Background" Value="{StaticResource Brush.Accent.Pressed}" />
          </Trigger>
          <Trigger Property="IsEnabled" Value="False">
            <Setter Property="Opacity" Value="0.6" />
          </Trigger>
        </Style.Triggers>
      </Style>

      <!-- Global style for TextBox -->
      <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="White" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
          <Trigger Property="IsKeyboardFocused" Value="True">
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
          </Trigger>
          <Trigger Property="IsEnabled" Value="False">
            <Setter Property="Background" Value="#FFEFEFEF" />
            <Setter Property="Foreground" Value="{StaticResource Brush.Disabled}" />
          </Trigger>
        </Style.Triggers>
      </Style>

      <!-- Global style for ComboBox -->
      <Style TargetType="{x:Type ComboBox}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Style.Triggers>
          <Trigger Property="IsKeyboardFocused" Value="True">
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
          </Trigger>
        </Style.Triggers>
      </Style>

      <!-- Styles for ListBox/ListView -->
      <Style TargetType="{x:Type ListBox}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="1" />
      </Style>
      <Style TargetType="{x:Type ListView}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="1" />
      </Style>
      <Style TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
        <Setter Property="Background" Value="{StaticResource Brush.Gray.Background}" />
        <Setter Property="Padding" Value="8,6" />
      </Style>

      <!-- Estilos para DataGrid (tabela) -->
      <Style TargetType="{x:Type DataGrid}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Base}" />
        <Setter Property="RowHeight" Value="28" />
        <Setter Property="ColumnHeaderHeight" Value="32" />
        <Setter Property="HeadersVisibility" Value="All" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="1" />
      </Style>
      <Style TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
        <Setter Property="Background" Value="{StaticResource Brush.Gray.Background}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="8,6" />
      </Style>
      <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Gray.Border}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
